# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# BaZi Engine (external service for chart analysis)
BAZI_ENGINE_URL=http://localhost:3002

# Session Security (REQUIRED for production)
# Generate with: openssl rand -base64 32
SESSION_SECRET=

# ElevenLabs Webhook Security (REQUIRED for production)
# Generate with: openssl rand -base64 32
ELEVENLABS_TOOL_SECRET=

# ElevenLabs Agent IDs (for voice agent integration)
ELEVENLABS_AGENT_ID_LEVI=your-levi-agent-id
ELEVENLABS_AGENT_ID_VICTORIA=your-victoria-agent-id

# Gemini API (for AI features)
GEMINI_API_KEY=your-gemini-api-key
# Google Maps API (for place autocomplete and geocoding)
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key


# Server Configuration
PORT=3001
NODE_ENV=development

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit actual secrets to git
# 2. SESSION_SECRET and ELEVENLABS_TOOL_SECRET are MANDATORY in production
# 3. Server will NOT start without SESSION_SECRET
# 4. Webhook endpoint will reject all requests without ELEVENLABS_TOOL_SECRET
# 5. Use strong random values (minimum 32 bytes base64-encoded)
# 6. SUPABASE_SERVICE_ROLE_KEY must NEVER be exposed to client (server-side only)
